apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: transactions
  namespace: transactions
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: transactions
  endpoints:
    - port: http
      path: /metrics
      interval: 15s