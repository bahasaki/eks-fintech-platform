apiVersion: v1
kind: Service
metadata:
  name: accounts
  namespace: accounts
  labels:
    app: accounts
spec:
  selector:
    app: accounts
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP